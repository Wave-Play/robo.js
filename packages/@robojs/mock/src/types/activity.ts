/**
 * Activity-related types for @robojs/mock.
 * Phase 1: Manifest types only.
 * Later phases add: ActivitySessionRecord, SubscriptionRegistry, etc.
 */

export type { RpcManifest, RpcCommandDefinition, RpcEventDefinition } from '../activity/schema/manifest-types.js'
