import { logger } from 'robo.js'

/**
 * This event handler will be called when your Robo is logged in and ready.
 *
 * Learn more about Discord events:
 * https://robojs.dev/discord-bots/events
 */
export default () => {
	logger.ready('Robo is ready!')
}
