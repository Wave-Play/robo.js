---
title: "getMockModeSession()"
---

# Function: getMockModeSession()

```ts
function getMockModeSession(): Session | null
```

Gets the current mock mode session (if any).
Used by the CLI command for shutdown handling.

## Returns

[`Session`](../classes/session) \| `null`
