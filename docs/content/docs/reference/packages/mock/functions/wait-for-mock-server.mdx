---
title: "waitForMockServer()"
---

# Function: waitForMockServer()

```ts
function waitForMockServer(options?): Promise<void>
```

Wait for the mock server to be ready

## Parameters

| Parameter | Type |
| ------ | ------ |
| `options`? | \{ `interval`: `number`; `timeout`: `number`; `url`: `string`; \} |
| `options.interval`? | `number` |
| `options.timeout`? | `number` |
| `options.url`? | `string` |

## Returns

`Promise`\<`void`\>
