---
title: "createMockGuild()"
---

# Function: createMockGuild()

```ts
function createMockGuild(config?): MockGuild
```

Create a mock guild from config

## Parameters

| Parameter | Type |
| ------ | ------ |
| `config`? | \{ `id`: `string`; `name`: `string`; `ownerId`: `string`; \} |
| `config.id`? | `string` |
| `config.name`? | `string` |
| `config.ownerId`? | `string` |

## Returns

[`MockGuild`](../interfaces/mock-guild)
