---
title: "resetSession()"
---

# Function: resetSession()

```ts
function resetSession(sessionId): Promise<void>
```

Reset a session to its initial state

## Parameters

| Parameter | Type |
| ------ | ------ |
| `sessionId` | `string` |

## Returns

`Promise`\<`void`\>
