---
title: "timestampToSnowflake()"
---

# Function: timestampToSnowflake()

```ts
function timestampToSnowflake(timestamp): string
```

Create a snowflake from a specific timestamp
Useful for creating IDs that sort correctly by time

## Parameters

| Parameter | Type |
| ------ | ------ |
| `timestamp` | `number` |

## Returns

`string`
