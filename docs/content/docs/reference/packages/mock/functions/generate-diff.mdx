---
title: "generateDiff()"
---

# Function: generateDiff()

```ts
function generateDiff(expected, actual): string
```

Generate a diff string for UI display

## Parameters

| Parameter | Type |
| ------ | ------ |
| `expected` | `unknown` |
| `actual` | `unknown` |

## Returns

`string`
