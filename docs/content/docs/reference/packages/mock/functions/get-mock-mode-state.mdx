---
title: "getMockModeState()"
---

# Function: getMockModeState()

```ts
function getMockModeState(): MockModeState
```

Gets the current mock mode state.
Used by the start hook to check if it should create a session.

## Returns

[`MockModeState`](../interfaces/mock-mode-state)
