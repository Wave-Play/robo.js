---
title: "createMockGuildMember()"
---

# Function: createMockGuildMember()

```ts
function createMockGuildMember(
   guildId, 
   userId, 
   config?): MockGuildMember
```

Create a mock guild member from config

## Parameters

| Parameter | Type |
| ------ | ------ |
| `guildId` | `string` |
| `userId` | `string` |
| `config`? | [`MockGuildMemberConfig`](../interfaces/mock-guild-member-config) |

## Returns

[`MockGuildMember`](../interfaces/mock-guild-member)
