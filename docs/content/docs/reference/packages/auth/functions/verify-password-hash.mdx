---
title: "~~verifyPasswordHash()~~"
---

# Function: ~~verifyPasswordHash()~~

```ts
function verifyPasswordHash(password, storedHash): Promise<boolean>
```

Validates a password against a stored Argon2id hash.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `password` | `string` |
| `storedHash` | `string` |

## Returns

`Promise`\<`boolean`\>

## Deprecated

Use `Argon2Hasher` or a custom `PasswordHasher` instead.
