---
title: "PasswordAdapter"
---

# Interface: PasswordAdapter

Auth.js adapter contract extended with password management helpers.

## Extends

- `Adapter`

## Methods

### createUserPassword()

```ts
createUserPassword(params): Promise<PasswordRecord>
```

Persists a hashed password for the given user and returns the storage record.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `params` | \{ `email`: `string`; `hash`: `string`; `userId`: `string`; \} |
| `params.email` | `string` |
| `params.hash` | `string` |
| `params.userId` | `string` |

#### Returns

`Promise`\<[`PasswordRecord`](password-record)\>

***

### deleteUserPassword()

```ts
deleteUserPassword(userId): Promise<void>
```

Removes any stored password material for a user.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `userId` | `string` |

#### Returns

`Promise`\<`void`\>

***

### findUserIdByEmail()

```ts
findUserIdByEmail(email): Promise<null | string>
```

Looks up the internal user id associated with an email address.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `email` | `string` |

#### Returns

`Promise`\<`null` \| `string`\>

***

### getUserPassword()

```ts
getUserPassword(userId): Promise<null | PasswordRecord>
```

Retrieves the password record for the given user.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `userId` | `string` |

#### Returns

`Promise`\<`null` \| [`PasswordRecord`](password-record)\>

***

### resetUserPassword()

```ts
resetUserPassword(params): Promise<null | PasswordRecord>
```

Replaces the existing password hash and returns the updated record.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `params` | \{ `hash`: `string`; `userId`: `string`; \} |
| `params.hash` | `string` |
| `params.userId` | `string` |

#### Returns

`Promise`\<`null` \| [`PasswordRecord`](password-record)\>
