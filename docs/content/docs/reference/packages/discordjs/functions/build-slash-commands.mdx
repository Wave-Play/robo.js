---
title: "buildSlashCommands()"
---

# Function: buildSlashCommands()

```ts
function buildSlashCommands(
   commands, 
   config?, 
   dev?): SlashCommandBuilder[]
```

Build slash commands for Discord API.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `commands` | `Record`\<`string`, [`CommandEntry`](../interfaces/command-entry)\> |
| `config`? | [`DiscordConfig`](../interfaces/discord-config) |
| `dev`? | `boolean` |

## Returns

`SlashCommandBuilder`[]
