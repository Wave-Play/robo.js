---
title: "executeAutocompleteHandler()"
---

# Function: executeAutocompleteHandler()

```ts
function executeAutocompleteHandler(interaction, commandKey): Promise<void>
```

Execute an autocomplete handler

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `interaction` | `AutocompleteInteraction`\<`CacheType`\> | The Discord autocomplete interaction |
| `commandKey` | `string` | The command key (e.g., "search" or "user find") |

## Returns

`Promise`\<`void`\>
