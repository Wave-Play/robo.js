---
title: "CommandOptions<ConfigType>"
---

# Type Alias: CommandOptions\<ConfigType\>

```ts
type CommandOptions<ConfigType> = { [K in NonNullable<ConfigType["options"]>[number] as K["name"]]: K extends { required: true } ? ValueOfOption<K> : ValueOfOption<K> | undefined };
```

Infer options type from command config.
Uses ValueOfOption from helpers for full type inference including choices.

## Type Parameters

| Type Parameter |
| ------ |
| `ConfigType` *extends* [`CommandConfig`](../interfaces/command-config) |
