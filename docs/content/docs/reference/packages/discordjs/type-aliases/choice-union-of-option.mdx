---
title: "ChoiceUnionOfOption<K>"
---

# Type Alias: ChoiceUnionOfOption\<K\>

```ts
type ChoiceUnionOfOption<K> = K extends {
  choices: readonly infer C[];
 } ? ChoiceValueOf<C> : never;
```

For an option `K`, produces a union of all `choices[].value` literals.
If no choices are declared, results in `never`.
Example: choices: [\{value:'red'\},\{value:'blue'\}] -\> 'red' | 'blue'

## Type Parameters

| Type Parameter |
| ------ |
| `K` |
