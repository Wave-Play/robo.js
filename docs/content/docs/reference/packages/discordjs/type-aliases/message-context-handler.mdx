---
title: "MessageContextHandler()"
---

# Type Alias: MessageContextHandler()

```ts
type MessageContextHandler = (interaction, message) => unknown | Promise<unknown>;
```

Context menu handler function type (message context).

## Parameters

| Parameter | Type |
| ------ | ------ |
| `interaction` | `MessageContextMenuCommandInteraction` |
| `message` | `Message` |

## Returns

`unknown` \| `Promise`\<`unknown`\>
