---
title: "UserContextHandler()"
---

# Type Alias: UserContextHandler()

```ts
type UserContextHandler = (interaction, user) => unknown | Promise<unknown>;
```

Context menu handler function type (user context).

## Parameters

| Parameter | Type |
| ------ | ------ |
| `interaction` | `UserContextMenuCommandInteraction` |
| `user` | `User` |

## Returns

`unknown` \| `Promise`\<`unknown`\>
