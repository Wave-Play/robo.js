---
title: "setCustomColumns()"
---

# Function: setCustomColumns()

```ts
function setCustomColumns(guildId, columns): void
```

Sets custom columns for a guild.

This helper replaces the default column set with custom columns.
If provided, these columns completely override the provider's default columns.

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `guildId` | `string` | The Discord guild ID |
| `columns` | \{ `archived`: `boolean`; `createForum`: `boolean`; `id`: `string`; `name`: `string`; `order`: `number`; \}[] | Array of custom column definitions |

## Returns

`void`

## Example

```ts
setCustomColumns(guildId, [
  { id: 'planning', name: 'Planning', order: 0 },
  { id: 'development', name: 'Development', order: 1 }
]);
```
