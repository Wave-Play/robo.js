---
title: "RoboRequest"
---

# Class: RoboRequest

Extends the [Web Request API](https://developer.mozilla.org/docs/Web/API/Request) with additional convenience methods.

## Extends

- `Request`

## Properties

| Property | Type |
| ------ | ------ |
| <a id="internals"></a> `[INTERNALS]` | \{ `params`: `Record`\<`string`, `string`\>; `raw`: `IncomingMessage`; \} |
| `[INTERNALS].params` | `Record`\<`string`, `string`\> |
| `[INTERNALS].raw` | `IncomingMessage` |

## Accessors

### params

#### Get Signature

```ts
get params(): Record<string, string>
```

##### Returns

`Record`\<`string`, `string`\>

***

### query

#### Get Signature

```ts
get query(): Record<string, string | string[]>
```

##### Returns

`Record`\<`string`, `string` \| `string`[]\>

***

### raw

#### Get Signature

```ts
get raw(): IncomingMessage
```

##### Returns

`IncomingMessage`

## Methods

### forTesting()

```ts
static forTesting(options): RoboRequest
```

Creates a RoboRequest for testing purposes without requiring an IncomingMessage.
This factory method allows creating fully-functional RoboRequest instances
with params, query, headers, and body for unit testing API handlers.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `options` | [`ForTestingOptions`](../interfaces/for-testing-options) |

#### Returns

[`RoboRequest`](robo-request)

#### Example

```typescript
const request = RoboRequest.forTesting({
  method: 'POST',
  path: '/users',
  params: { id: '123' },
  query: { include: 'profile' },
  body: { name: 'John' }
})
const response = await handler(request)
```

***

### from()

```ts
static from(req, options?): Promise<RoboRequest>
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `req` | `IncomingMessage` |
| `options`? | `FromOptions` |

#### Returns

`Promise`\<[`RoboRequest`](robo-request)\>
