---
title: "Server"
---

# Variable: Server

```ts
const Server: {
  config: () => unknown;
  get: () => BaseEngine | undefined;
  ready: () => Promise<void>;
};
```

Use this to interact with the server.

## Type declaration

| Name | Type |
| ------ | ------ |
| <a id="config"></a> `config` | () =\> `unknown` |
| <a id="get"></a> `get` | () =\> `BaseEngine` \| `undefined` |
| <a id="ready"></a> `ready` | () =\> `Promise`\<`void`\> |
