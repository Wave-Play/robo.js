---
title: "initPluginRoutes()"
---

# Function: initPluginRoutes()

```ts
function initPluginRoutes(pluginPrefixes?): PluginRouteRegistry
```

Initialize the plugin route registry with prefix configuration.
Called during server startup.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `pluginPrefixes`? | [`PluginPrefixMap`](../type-aliases/plugin-prefix-map) |

## Returns

`PluginRouteRegistry`
