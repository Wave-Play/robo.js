---
title: "Robo"
---

# Variable: Robo

```ts
const Robo: {
  build: (options?) => Promise<void>;
  restart: () => Promise<void>;
  start: (options?) => Promise<void>;
  stop: (exitCode, reason?) => Promise<void>;
};
```

Robo is the main entry point for your app. It provides a simple API for starting, stopping, and restarting your Robo.

```ts
import { Robo } from 'robo.js'

Robo.start()
```

You do not normally need to use this API directly, as the CLI will handle starting and stopping for you.

[**Learn more:** Robo](https://robojs.dev/robojs/overview)

## Type declaration

| Name | Type |
| ------ | ------ |
| <a id="build"></a> `build` | (`options`?) =\> `Promise`\<`void`\> |
| <a id="restart"></a> `restart` | () =\> `Promise`\<`void`\> |
| <a id="start"></a> `start` | (`options`?) =\> `Promise`\<`void`\> |
| <a id="stop"></a> `stop` | (`exitCode`, `reason`?) =\> `Promise`\<`void`\> |
