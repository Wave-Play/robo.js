---
title: "env"
---

# Variable: env

```ts
const env: Env<{
  boot: {
     disableCheck: {
        env: string;
       };
    };
  nodeEnv: {
     env: string;
    };
  roboplay: {
     api: {
        default: string;
        env: string;
       };
     debug: {
        env: string;
       };
     env: {
        env: string;
       };
     frontend: {
        default: string;
        env: string;
       };
    };
}>;
```
