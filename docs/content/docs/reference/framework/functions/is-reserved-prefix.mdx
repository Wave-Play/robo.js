---
title: "isReservedPrefix()"
---

# Function: isReservedPrefix()

```ts
function isReservedPrefix(key): boolean
```

Check if a key starts with a reserved prefix.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `string` |

## Returns

`boolean`
