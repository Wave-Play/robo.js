---
title: "hasIncludes()"
---

# Function: hasIncludes()

```ts
function hasIncludes(includeClause?): boolean
```

Check if an include clause has any entries.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `includeClause`? | `IncludeClause` |

## Returns

`boolean`
