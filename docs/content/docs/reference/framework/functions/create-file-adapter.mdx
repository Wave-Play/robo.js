---
title: "createFileAdapter()"
---

# Function: createFileAdapter()

```ts
function createFileAdapter<K, V>(options?): FileAdapter<K, V>
```

Create a new FileAdapter instance.

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `K` *extends* `string` | `string` |
| `V` | `unknown` |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `options`? | [`FileAdapterOptions`](../interfaces/file-adapter-options) |

## Returns

[`FileAdapter`](../classes/file-adapter)\<`K`, `V`\>
