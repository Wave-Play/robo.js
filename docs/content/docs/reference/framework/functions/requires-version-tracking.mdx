---
title: "requiresVersionTracking()"
---

# Function: requiresVersionTracking()

```ts
function requiresVersionTracking(mode): boolean
```

Determine if a mode requires version tracking for conflict detection.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `mode` | [`ResolvedTransactionMode`](../type-aliases/resolved-transaction-mode) |

## Returns

`boolean`
