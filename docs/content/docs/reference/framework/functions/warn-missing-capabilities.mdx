---
title: "warnMissingCapabilities()"
---

# Function: warnMissingCapabilities()

```ts
function warnMissingCapabilities(capabilities, logger): void
```

Log warnings for missing recommended capabilities.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `capabilities` | [`AdapterCapabilities`](../interfaces/adapter-capabilities) |
| `logger` | \{ `warn`: (`msg`) =\> `void`; \} |
| `logger.warn` | (`msg`) =\> `void` |

## Returns

`void`
