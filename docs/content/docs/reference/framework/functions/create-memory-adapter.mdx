---
title: "createMemoryAdapter()"
---

# Function: createMemoryAdapter()

```ts
function createMemoryAdapter<K, V>(): MemoryAdapter<K, V>
```

Create a new MemoryAdapter instance.

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `K` *extends* `string` | `string` |
| `V` | `unknown` |

## Returns

[`MemoryAdapter`](../classes/memory-adapter)\<`K`, `V`\>
