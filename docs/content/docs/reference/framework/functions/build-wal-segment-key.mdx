---
title: "buildWalSegmentKey()"
---

# Function: buildWalSegmentKey()

```ts
function buildWalSegmentKey(walId, segmentIndex): string
```

Build a WAL segment key.

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `walId` | `string` | The WAL entry ID |
| `segmentIndex` | `number` | The segment index |

## Returns

`string`

WAL segment key
