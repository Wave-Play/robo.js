---
title: "createPluginState()"
---

# Function: createPluginState()

```ts
function createPluginState(pluginName): PluginState
```

Create a plugin-scoped state instance.
Uses namespaced keys to isolate plugin data.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `pluginName` | `string` |

## Returns

[`PluginState`](../interfaces/plugin-state)
