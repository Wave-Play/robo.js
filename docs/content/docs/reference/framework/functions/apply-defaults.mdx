---
title: "applyDefaults()"
---

# Function: applyDefaults()

```ts
function applyDefaults(data, schema): Record<string, unknown>
```

Apply default values to a record.

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `data` | `Record`\<`string`, `unknown`\> | Input data |
| `schema` | [`NormalizedSchema`](../interfaces/normalized-schema) | Normalized schema |

## Returns

`Record`\<`string`, `unknown`\>

Data with defaults applied
