---
title: "createChunkManager()"
---

# Function: createChunkManager()

```ts
function createChunkManager(options): ChunkManager
```

Create a chunk manager for a model.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `options` | `ChunkManagerOptions` |

## Returns

[`ChunkManager`](../classes/chunk-manager)
