---
title: "getWALManager()"
---

# Function: getWALManager()

```ts
function getWALManager(): WriteAheadLog | null
```

Get the global WAL manager instance.
Returns null if WAL is not initialized or disabled.

## Returns

[`WriteAheadLog`](../classes/write-ahead-log) \| `null`
