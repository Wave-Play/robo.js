---
title: "hasPlugin()"
---

# Function: hasPlugin()

```ts
function hasPlugin(pluginName): boolean
```

Check if a plugin is registered.

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `pluginName` | `string` | Name of the plugin |

## Returns

`boolean`

True if plugin is registered
