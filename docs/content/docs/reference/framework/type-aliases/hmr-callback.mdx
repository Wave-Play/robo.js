---
title: "HmrCallback()"
---

# Type Alias: HmrCallback()

```ts
type HmrCallback = (context) => void | Promise<void>;
```

Callback function for HMR event subscriptions.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `context` | [`HmrEventContext`](../interfaces/hmr-event-context) |

## Returns

`void` \| `Promise`\<`void`\>
