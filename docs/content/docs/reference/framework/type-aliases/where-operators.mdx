---
title: "WhereOperators<T>"
---

# Type Alias: WhereOperators\<T\>

```ts
type WhereOperators<T> = {
  contains: string;
  endsWith: string;
  equals: T;
  gt: T;
  gte: T;
  in: T[];
  lt: T;
  lte: T;
  not: T;
  startsWith: string;
};
```

Where clause operators for filtering.

## Type Parameters

| Type Parameter |
| ------ |
| `T` |

## Type declaration

| Name | Type |
| ------ | ------ |
| <a id="contains"></a> `contains`? | `string` |
| <a id="endswith"></a> `endsWith`? | `string` |
| <a id="equals"></a> `equals`? | `T` |
| <a id="gt"></a> `gt`? | `T` |
| <a id="gte"></a> `gte`? | `T` |
| <a id="in"></a> `in`? | `T`[] |
| <a id="lt"></a> `lt`? | `T` |
| <a id="lte"></a> `lte`? | `T` |
| <a id="not"></a> `not`? | `T` |
| <a id="startswith"></a> `startsWith`? | `string` |
