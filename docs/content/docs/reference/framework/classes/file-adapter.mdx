---
title: "FileAdapter<K, V>"
---

# Class: FileAdapter\<K, V\>

File-based storage adapter using the filesystem.

Features:
- Atomic writes via temp file + rename
- Filesystem-safe key encoding
- scan(prefix) via directory listing
- setIfNotExists via exclusive file creation

Storage layout:

<Files>
  <Folder name=".robo" defaultOpen disabled>
    <Folder name="flashcore" defaultOpen>
      <File name="{encoded-key}.json" />
      <File name="{encoded-key}.json" />
    </Folder>
  </Folder>
</Files>

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `K` *extends* `string` | `string` |
| `V` | `unknown` |

## Implements

- [`FlashcoreAdapter`](../interfaces/flashcore-adapter)\<`K`, `V`\>

## Constructors

### new FileAdapter()

```ts
new FileAdapter<K, V>(options): FileAdapter<K, V>
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `options` | [`FileAdapterOptions`](../interfaces/file-adapter-options) |

#### Returns

[`FileAdapter`](file-adapter)\<`K`, `V`\>

## Properties

| Property | Modifier | Type | Default value | Description |
| ------ | ------ | ------ | ------ | ------ |
| <a id="name"></a> `name` | `readonly` | `"FileAdapter"` | `'FileAdapter'` | Human-readable adapter name for logging and introspection. |

## Methods

### atomicBatch()

```ts
atomicBatch(ops): Promise<void>
```

Apply a batch of operations atomically.

Note: This is NOT truly atomic at the filesystem level.
For crash safety, use WAL at a higher level.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `ops` | [`BatchOperation`](../type-aliases/batch-operation)\<`K`, `V`\>[] |

#### Returns

`Promise`\<`void`\>

#### Implementation of

[`FlashcoreAdapter`](../interfaces/flashcore-adapter).[`atomicBatch`](../interfaces/flashcore-adapter#atomicbatch)

***

### capabilities()

```ts
capabilities(): AdapterCapabilitiesReport
```

Report extended capabilities.

#### Returns

[`AdapterCapabilitiesReport`](../interfaces/adapter-capabilities-report)

#### Implementation of

[`FlashcoreAdapter`](../interfaces/flashcore-adapter).[`capabilities`](../interfaces/flashcore-adapter#capabilities)

***

### clear()

```ts
clear(): Promise<void>
```

Clear all data.

#### Returns

`Promise`\<`void`\>

#### Implementation of

[`FlashcoreAdapter`](../interfaces/flashcore-adapter).[`clear`](../interfaces/flashcore-adapter#clear)

***

### compareAndSwap()

```ts
compareAndSwap(
   key, 
   expected, 
next): Promise<boolean>
```

Compare and swap: atomically update if current value matches expected.

Note: This implementation is NOT truly atomic on the filesystem level,
but uses in-memory comparison. For true atomicity, use with chunk/catalog locks.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `K` |
| `expected` | `V` |
| `next` | `V` |

#### Returns

`Promise`\<`boolean`\>

#### Implementation of

[`FlashcoreAdapter`](../interfaces/flashcore-adapter).[`compareAndSwap`](../interfaces/flashcore-adapter#compareandswap)

***

### delete()

```ts
delete(key): Promise<boolean>
```

Delete a key.
Returns true if the key existed and was deleted.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `K` |

#### Returns

`Promise`\<`boolean`\>

#### Implementation of

[`FlashcoreAdapter`](../interfaces/flashcore-adapter).[`delete`](../interfaces/flashcore-adapter#delete)

***

### get()

```ts
get(key): Promise<V>
```

Get a value by key.
Returns undefined if the key does not exist.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `K` |

#### Returns

`Promise`\<`V`\>

#### Implementation of

[`FlashcoreAdapter`](../interfaces/flashcore-adapter).[`get`](../interfaces/flashcore-adapter#get)

***

### getBaseDir()

```ts
getBaseDir(): string
```

Get the storage directory path.

#### Returns

`string`

***

### has()

```ts
has(key): Promise<boolean>
```

Check if a key exists.
Must return true for stored falsy values (0, false, '', null).

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `K` |

#### Returns

`Promise`\<`boolean`\>

#### Implementation of

[`FlashcoreAdapter`](../interfaces/flashcore-adapter).[`has`](../interfaces/flashcore-adapter#has)

***

### init()

```ts
init(): Promise<void>
```

Initialize the adapter (e.g., connect, create directories).
Called once during Flashcore.$.init().

#### Returns

`Promise`\<`void`\>

#### Implementation of

[`FlashcoreAdapter`](../interfaces/flashcore-adapter).[`init`](../interfaces/flashcore-adapter#init)

***

### keys()

```ts
keys(): Promise<K[]>
```

Get all stored keys (for testing/debugging).

#### Returns

`Promise`\<`K`[]\>

***

### scan()

```ts
scan(prefix): Promise<K[]>
```

Scan for keys with a given prefix.
Scans the directory and filters by prefix after decoding filenames.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `prefix` | `K` |

#### Returns

`Promise`\<`K`[]\>

#### Implementation of

[`FlashcoreAdapter`](../interfaces/flashcore-adapter).[`scan`](../interfaces/flashcore-adapter#scan)

***

### set()

```ts
set(key, value): Promise<boolean>
```

Set a key-value pair.
Returns true if the operation succeeded.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `K` |
| `value` | `V` |

#### Returns

`Promise`\<`boolean`\>

#### Implementation of

[`FlashcoreAdapter`](../interfaces/flashcore-adapter).[`set`](../interfaces/flashcore-adapter#set)

***

### setIfNotExists()

```ts
setIfNotExists(key, value): Promise<boolean>
```

Set a key only if it doesn't exist.
Uses exclusive file creation flag to ensure atomicity.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `K` |
| `value` | `V` |

#### Returns

`Promise`\<`boolean`\>

#### Implementation of

[`FlashcoreAdapter`](../interfaces/flashcore-adapter).[`setIfNotExists`](../interfaces/flashcore-adapter#setifnotexists)

***

### shutdown()

```ts
shutdown(): Promise<void>
```

Shutdown the adapter (e.g., close connections).

#### Returns

`Promise`\<`void`\>

#### Implementation of

[`FlashcoreAdapter`](../interfaces/flashcore-adapter).[`shutdown`](../interfaces/flashcore-adapter#shutdown)

***

### size()

```ts
size(): Promise<number>
```

Get the number of stored keys.

#### Returns

`Promise`\<`number`\>
