---
title: Multiplayer
description: Add real-time multiplayer features to your Discord Activity
---

**[`@robojs/sync`](./sync-api)** provides real-time state synchronization for Discord Activities. It syncs state across all connected clients using WebSockets.

## Setup

Install the plugin:

```package-install
npx robo add @robojs/sync
```

Wrap your app in `SyncContextProvider`. This requires `@robojs/server`, which is already included in scaffolded activity projects.

<Tabs items={["TypeScript", "JavaScript"]} groupId="language" persist>
<Tab value="TypeScript">

  ```tsx title="src/app/App.tsx"
  import { SyncContextProvider } from '@robojs/sync'

  export function App() {
    return (
      <SyncContextProvider>
        <Activity />
      </SyncContextProvider>
    )
  }
  ```

</Tab>
<Tab value="JavaScript">

  ```jsx title="src/app/App.jsx"
  import { SyncContextProvider } from '@robojs/sync'

  export function App() {
    return (
      <SyncContextProvider>
        <Activity />
      </SyncContextProvider>
    )
  }
  ```

</Tab>
</Tabs>

## Shared State

`useSyncState` works like React's `useState` but syncs across all connected clients. Think of keys like channel names — components using the same key share the same state.

<Tabs items={["TypeScript", "JavaScript"]} groupId="language" persist>
<Tab value="TypeScript">

  ```tsx title="src/app/Counter.tsx"
  import { useSyncState } from '@robojs/sync'

  function Counter() {
    const [count, setCount] = useSyncState(0, ['counter'])

    return (
      <button onClick={() => setCount((prev) => prev + 1)}>
        Count: {count}
      </button>
    )
  }
  ```

</Tab>
<Tab value="JavaScript">

  ```jsx title="src/app/Counter.jsx"
  import { useSyncState } from '@robojs/sync'

  function Counter() {
    const [count, setCount] = useSyncState(0, ['counter'])

    return (
      <button onClick={() => setCount((prev) => prev + 1)}>
        Count: {count}
      </button>
    )
  }
  ```

</Tab>
</Tabs>

<ScreenshotPlaceholder
  alt="Two browser windows side by side showing the same activity with a synchronized counter, where clicking in one window updates both"
  focus="Two windows showing the same counter value in real-time sync"
  zoom="100%"
  aspect="16/9"
  notes="Show two browser windows running the same activity. Both display 'Count: 5' button. The key message: both show the same value because useSyncState synchronizes across clients."
/>

## Key Arrays

Keys determine which clients share state. Different key structures create different scopes.

| Key | Scope |
|-----|-------|
| `['counter']` | All clients share one counter |
| `['player', userId]` | Per-user state |
| `['room', roomId, 'chat']` | Per-room chat |

## Updating State

Use updater functions for concurrent safety:

<Tabs items={["TypeScript", "JavaScript"]} groupId="language" persist>
<Tab value="TypeScript">

  ```ts title="src/app/Counter.tsx"
  setCount((prev) => prev + 1)
  ```

</Tab>
<Tab value="JavaScript">

  ```js title="src/app/Counter.jsx"
  setCount((prev) => prev + 1)
  ```

</Tab>
</Tabs>

Partial updates merge with existing state for objects. For example, updating only the `x` field leaves `y` unchanged:

<Tabs items={["TypeScript", "JavaScript"]} groupId="language" persist>
<Tab value="TypeScript">

  ```ts title="src/app/Game.tsx"
  const [position, setPosition] = useSyncState({ x: 0, y: 0 }, ['position'])
  setPosition({ x: 100 }) // y remains 0
  ```

</Tab>
<Tab value="JavaScript">

  ```js title="src/app/Game.jsx"
  const [position, setPosition] = useSyncState({ x: 0, y: 0 }, ['position'])
  setPosition({ x: 100 }) // y remains 0
  ```

</Tab>
</Tabs>

## Client Awareness

The third return value from `useSyncState` is a context object with information about connected clients.

<Tabs items={["TypeScript", "JavaScript"]} groupId="language" persist>
<Tab value="TypeScript">

  ```ts title="src/app/Activity.tsx"
  const [state, setState, context] = useSyncState(initialState, ['room'])
  ```

</Tab>
<Tab value="JavaScript">

  ```js title="src/app/Activity.jsx"
  const [state, setState, context] = useSyncState(initialState, ['room'])
  ```

</Tab>
</Tabs>

| Field | Type | Description |
|-------|------|-------------|
| `clients` | Client[] | All connected clients |
| `clientId` | string | Current client's ID |
| `isHost` | boolean | Whether current client is host |
| `broadcast` | function | Send ephemeral message to all clients |
| `send` | function | Send ephemeral message to a specific client |

<ScreenshotPlaceholder
  alt="A Discord Activity showing connected users with a participant count and host indicator, demonstrating client awareness information"
  focus="The client awareness UI showing participants and host indicator"
  zoom="100%"
  aspect="16/9"
  notes="Show an activity displaying: list of connected users, count like '3 players connected', and host indicator (crown or 'Host' label). Demonstrates context object."
/>

## Colyseus

For most activities, `@robojs/sync` is sufficient. Consider Colyseus for complex games needing server-authoritative state — where the server is the single source of truth and validates every action, preventing cheating.

Start from a template:

```package-install
npx create-robo my-activity --template discord-activities/react-colyseus-ts
```

Colyseus is a dedicated multiplayer game server with schema-based state, room management, and reconnection support. See the [Colyseus documentation](https://docs.colyseus.io/) for details.

## Choosing a Solution

| | @robojs/sync | Colyseus |
|---|---|---|
| Setup | One command | Manual configuration |
| Server logic | None required | Room-based handlers |
| State model | Last-write-wins (the most recent update from any client overwrites previous values) | Authoritative server (the server validates and controls all state changes) |
| Persistence | In-memory | Configurable |
| Best for | Prototypes, simple sync | Complex games, scaling |

<ScreenshotPlaceholder
  alt="A multiplayer activity inside Discord with multiple users interacting simultaneously, showing real-time state updates like shared cursors or a collaborative interface"
  focus="Multiple users interacting in real-time within the same activity"
  zoom="100%"
  aspect="16/9"
  notes="Show an activity in Discord's embedded panel with visual evidence of multiplayer: multiple cursors, shared drawing, or game board. Include Discord voice channel UI for context."
/>

## Next Steps

<Cards>
  <Card
    href="./sync-api"
    title="Sync API"
    description="Full API reference for @robojs/sync."
  />
  <Card
    href="./backend"
    title="Backend"
    description="Server-side logic and API routes."
  />
  <Card
    href="./configuration"
    title="Configuration"
    description="Configure your activity project."
  />
</Cards>
