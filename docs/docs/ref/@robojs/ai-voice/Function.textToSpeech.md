# Function: textToSpeech()

```ts
function textToSpeech(text, filePath): Promise<string>
```

## Parameters

| Parameter | Type |
| ------ | ------ |
| `text` | `string` |
| `filePath` | `string` |

## Returns

`Promise`\<`string`\>
