# Type Alias: Hook()

```ts
type Hook: (context, iteration) => Promise<ChatMessage[]>;
```

## Parameters

| Parameter | Type |
| ------ | ------ |
| `context` | `HookContext` |
| `iteration` | `number` |

## Returns

`Promise`\<[`ChatMessage`](Interface.ChatMessage.md)[]\>
