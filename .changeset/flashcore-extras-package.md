---
'@robojs/flashcore-extras': minor
---

feat(flashcore-extras): extract advanced features into standalone package

New plugin package containing adapter wrappers (cache, compression, encryption, resilience), migration system, integrity checks, transaction support, WAL recovery, and database CLI commands previously bundled in robo.js core.
